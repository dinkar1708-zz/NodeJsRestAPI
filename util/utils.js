//utility methods
module.exports = {
	/**
	 * return sum of two numbers
	 * 
	 * @param a
	 * @param b
	 * @returns
	 */
	addTwoNumber : function(a, b) {
		return parseInt(a) + parseInt(b);
	}
};